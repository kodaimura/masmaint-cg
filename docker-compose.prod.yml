services:
  app:
    build:
      context: .
    ports:
      - "3400:3400"
    volumes:
      - .:/usr/src/app
    working_dir: /usr/src/app
    tty: true
    command: sh -c "go mod tidy && go build cmd/masmaint-cg/main.go && ./main"
    restart: always